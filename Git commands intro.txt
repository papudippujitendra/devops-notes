 GIT COMMANDS INTRODUCTION 

How to rename the Default branch name ?
The default branch is considered the "base" branch in your Repository against which all push requests and code commits automatically made unless you specify a different branch.

git clone  we should give path <xxxxxxxxxxxx>
ls
git status
touch filename  ( to create file name)
cat >>filename ( to create file and we add the data)

vi filename   press enter then press i(insert) then we add or edit data press  enter after pressing enter press esc(escspe key) the enter :wq! press enter 
cat filename ( we can see the data)
git status 
git add filename
git commit -m "xxxxxxxxx"
git push 

git log  : in git log we can see 4 parameter  ( commit id , Author , date(time) commited message )

How to create new  branch 
git branch  branchname
 
git branch      -list out the branch
git switch branchname     -one branch to another branch
git branch -d branch name   -to delete branch name

git stash   -stash the changes in a dirty woorking directory away ( To pause the work)
git stash list  -To show the list of paused works
git stash pop  -to quiet the resume work
 